# Oliver AIKU Memory Vault

A static implementation of the attached Stitch reference design.

Open `index.html` in a browser to view the project. The page uses local artwork crops from the supplied screenshot, plus CSS glassmorphism, ambient glow, and a small hover tilt interaction.
